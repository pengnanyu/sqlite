{
  "name": "bms-sqlite",
  "version": "1.0.0",
  "description": "AIBMS ESA Edge Functions - Protocol Database API & Admin",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "build": "npx esbuild src/index.ts --bundle --outfile=dist/_worker.js --format=esm --platform=neutral --target=es2022 --define:process.env.NODE_ENV='\"production\"' --loader:.html=text && node -e \"require('fs').copyFileSync('dist/_worker.js','dist/index.js')\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.60.0"
  },
  "dependencies": {}
}
